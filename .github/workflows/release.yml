name: Release

on:
  push:
    tags: ["v*"]

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  cli:
    name: rustybolt ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: macos-14,         target: aarch64-apple-darwin,       name: darwin_arm64  }
          - { os: macos-13,         target: x86_64-apple-darwin,        name: darwin_amd64  }
          - { os: ubuntu-latest,    target: x86_64-unknown-linux-gnu,   name: linux_amd64   }
          - { os: ubuntu-24.04-arm, target: aarch64-unknown-linux-gnu,  name: linux_arm64   }
          - { os: windows-latest,   target: x86_64-pc-windows-msvc,     name: windows_amd64 }
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2

      - name: Build
        run: cargo build --release --locked -p bolt-cli --target ${{ matrix.target }}

      - name: Archive
        shell: bash
        run: |
          version="${GITHUB_REF_NAME#v}"
          stem="rustybolt_${version}_${{ matrix.name }}"
          mkdir "$stem"
          cp README.md CONTRACT.md "$stem/"
          if [ "${{ runner.os }}" = "Windows" ]; then
            cp "target/${{ matrix.target }}/release/rustybolt.exe" "$stem/"
            7z a -tzip "$stem.zip" "$stem" >/dev/null
          else
            cp "target/${{ matrix.target }}/release/rustybolt" "$stem/"
            tar -czf "$stem.tar.gz" "$stem"
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: cli-${{ matrix.name }}
          path: |
            rustybolt_*.tar.gz
            rustybolt_*.zip
          if-no-files-found: error

  app:
    name: rustyBolt.app
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build bundle
        run: macos/build-app.sh "$PWD/bundle"

      - name: Archive
        run: |
          version="${GITHUB_REF_NAME#v}"
          ditto -c -k --keepParent bundle/rustyBolt.app "rustyBolt_${version}_darwin_arm64.app.zip"

      - uses: actions/upload-artifact@v4
        with:
          name: app-darwin-arm64
          path: rustyBolt_*.app.zip
          if-no-files-found: error

  release:
    name: Draft release
    needs: [cli, app]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: assets
          merge-multiple: true

      - name: Checksums
        working-directory: assets
        run: sha256sum * > checksums.txt

      - name: Create draft release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --repo "$GITHUB_REPOSITORY" \
            --draft \
            --title "rustyBolt ${GITHUB_REF_NAME#v}" \
            --generate-notes \
            assets/*
